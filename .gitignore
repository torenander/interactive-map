# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Env
.env
.env.local

# Build / deps (för när stacken väljs)
node_modules/
dist/
build/

# Claude Code — lokala, icke-delade inställningar
.claude/settings.local.json

# Basemap build artifact — rebuild with scripts/fetch-tiles.sh
public/tiles/*.pmtiles
# Exception: the production maxzoom-14 extract is tracked and deployed
# same-origin via GitHub Pages (see docs/DEPLOY.md § Production) — the dev
# maxzoom-15 london.pmtiles above stays untracked/local-only.
!public/tiles/london-z14.pmtiles

# Playwright
/test-results/
/playwright-report/
